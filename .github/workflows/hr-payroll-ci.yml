name: hr-payroll CI

on:
  push:
    paths:
      - "hr-payroll/**"
      - ".github/workflows/hr-payroll-ci.yml"
  pull_request:
    paths:
      - "hr-payroll/**"
      - ".github/workflows/hr-payroll-ci.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: hr-payroll
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
      - run: chmod +x mvnw && ./mvnw -B verify
