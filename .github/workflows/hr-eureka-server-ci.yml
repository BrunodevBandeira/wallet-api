name: hr-eureka-server CI

on:
  push:
    paths:
      - "hr-eureka-server/**"
      - ".github/workflows/hr-eureka-server-ci.yml"
  pull_request:
    paths:
      - "hr-eureka-server/**"
      - ".github/workflows/hr-eureka-server-ci.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: hr-eureka-server
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
      - run: chmod +x mvnw && ./mvnw -B verify
